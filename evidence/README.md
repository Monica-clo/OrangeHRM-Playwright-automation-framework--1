# Test execution evidence

After a successful run, execute `python tools/collect_evidence.py`. It copies the HTML report,
the test videos and any screenshots into a `run_<date>` folder here, so they can be committed
to GitHub as the assessment requires.

Test run video (if it is too large for GitHub): <add Google Drive link here>
